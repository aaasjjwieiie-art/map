services:
  - type: web
    name: komekmap
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers=1 --threads=4 --preload
    envVars:
      - key: TELEGRAM_CHAT_ID
        value: "1748998701"
      - key: BOT_TOKEN
        sync: false
